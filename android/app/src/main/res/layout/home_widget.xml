<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widgetRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="14dp"
    android:background="@drawable/widget_glass_bg">

    <!-- App Name (Top Left) -->
    <TextView
        android:id="@+id/appTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="QuoteVault"
        android:textColor="#99FFFFFF"
        android:textSize="12sp"
        android:textStyle="bold"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"/>

    <!-- Quote Text (Positioned below App Title with some margin) -->
    <TextView
        android:id="@+id/quoteText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Quote of the Day will appear here"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:lineSpacingExtra="4dp"
        android:layout_below="@id/appTitle"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="12dp"/>

    <!-- Author (Stays below Quote) -->
    <TextView
        android:id="@+id/authorText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="- Author"
        android:textColor="#99FFFFFF"
        android:textSize="12sp"
        android:layout_below="@id/quoteText"
        android:layout_marginTop="8dp"
        android:layout_alignParentEnd="true"/>

    <!-- Footer (Stuck to the bottom) -->
    <TextView
        android:id="@+id/footerText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Quote of the Day"
        android:textColor="#66FFFFFF"
        android:textSize="11sp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"/>
</RelativeLayout>
